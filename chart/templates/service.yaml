apiVersion: v1
kind: Service
metadata:
  name: {{ include "shopify-service.fullname" . }}
  labels:
    {{- include "shopify-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  loadBalancerIP: 34.123.101.193
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shopify-service.selectorLabels" . | nindent 4 }}
